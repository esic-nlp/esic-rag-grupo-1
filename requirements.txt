# ══════════════════════════════════════════════════════════
#  RAG Nutricional DIA - requirements.txt
# ══════════════════════════════════════════════════════════

# ── Sistema RAG ──────────────────────────────────────────
faiss-cpu>=1.7.4            # Búsqueda vectorial eficiente (CPU)
sentence-transformers>=2.2.2 # Generación de embeddings semánticos
numpy>=1.24.0               # Operaciones numéricas

# ── Datos y análisis ─────────────────────────────────────
pandas>=2.0.0               # Manipulación de DataFrames
scipy>=1.10.0               # Científico (dependencia de faiss/sklearn)

# ── Scraping / Adquisición ───────────────────────────────
requests>=2.31.0            # Peticiones HTTP
beautifulsoup4>=4.12.0      # Parsing HTML (fallback scraping)
lxml>=4.9.0                 # Parser XML/HTML rápido para BS4

# ── Utilidades ───────────────────────────────────────────
tqdm>=4.66.0                # Barras de progreso
python-dotenv>=1.0.0        # Variables de entorno (opcional)

# ── Torch (necesario para sentence-transformers) ─────────
# Instala la versión CPU para reducir tamaño (sin GPU)
torch>=2.0.0
torchvision>=0.15.0
